#!/usr/bin/env bash
<<<<<<< HEAD
# This script displays the PID and process name of processes containing the word 'bash'.

# Iterate over processes with 'bash' in their name
for pid in $(pgrep -f bash); do
    # Get the process name associated with the PID
    process_name=$(ps -p "$pid" -o comm=)
    echo "$pid $process_name"
done
=======
# This script displays the PID and process name of processes whose name contains the word "bash."

# Get the current script's PID
script_pid=$$

# Iterate through processes and filter by the word "bash"
while read -r line; do
    pid=$(echo "$line" | awk '{print $2}')
    process_name=$(echo "$line" | awk '{print $11}')
    echo "$pid $process_name"
done < <(pgrep -af bash | grep -v "$script_pid")
>>>>>>> bc12d6d9fe2d4f172cd03cd8c0786d88eded4110

